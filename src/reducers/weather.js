//import { } from '../actions/'

const weather = (state = 0, action) => {
  switch (action.type) {
    /*case SET_COUNTER:
      return action.payload*/
    default:
      return state
  }
}

export default weather;